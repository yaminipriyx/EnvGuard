# EnvGuard
EnvGuard is a zero-knowledge CLI tool that securely provisions .env secrets directly into runtime memory using AES-256-GCM and ECDH key exchange, eliminating plaintext credentials on disk.
